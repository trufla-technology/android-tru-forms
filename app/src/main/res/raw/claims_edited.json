{
  "title": "Change Phone Number",
  "description": "Change Phone Number",
  "type": "object",
  "properties": {
    "new_phone_number": {
      "type": "string"
    },
    "phone_type": {
      "type": "string",
      "enum": [
        "home",
        "cell",
        "other"
      ],
      "enumNames": [
        "Home",
        "Cell",
        "Other"
      ]
    },
    "phone_type_other": {
      "type": "string"
    }
  },
  "required": [
    "new_phone_number",
    "phone_type"
  ],
  "oneOf": [{
    "properties": {
      "phone_type": {
        "enum": [
          "other"
        ]
      }
    },
    "required": [
      "phone_type_other"
    ]
  }]
}